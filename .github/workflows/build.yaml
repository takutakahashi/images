name: "build all"

on:
  push:

jobs:
  build:
    uses: takutakahashi/github-actions/.github/workflows/ghcr.yaml@main
    with:
      path: pvr-base
      image: takutakahashi/pvr-base:${{ github.sha }}
  test:
    needs: build
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/takutakahashi/pvr-base:${{ github.sha }}
    steps:
      -
        run: /px4.sh

